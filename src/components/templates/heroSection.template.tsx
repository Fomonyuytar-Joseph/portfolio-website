import React from 'react';
import HeroSectionOrganism from '../organisms/heroSection.organism';

const HeroSectionTemplate = () => {
  return (
    <div className="w-full h-screen mt-20">
      <HeroSectionOrganism />
    </div>
  );
};

export default HeroSectionTemplate;
